<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Honpe_SQE" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/blue_dark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowBackground">@android:color/white</item>
    </style>
    <style name="CustomPopWindowStyle">
        <item name="android:windowEnterAnimation">@anim/from_up_to_bottom</item>
        <item name="android:windowExitAnimation">@anim/from_bottom_to_up</item>
    </style>
    <style name="InputStyle">
        <item name="android:textColorHint">@color/smssdk_hint_textcolor</item>
        <item name="android:textSize">@dimen/_14sp</item>
        <!-- 底部线 默认颜色 -->
        <item name="colorControlNormal">@color/grey_e</item>
        <!-- 底部线 EditText激活时颜色 -->
        <item name="colorControlActivated">@color/grey_e</item>

        <!-- 设置光标 -->
        <item name="android:textCursorDrawable">@drawable/color_cursor_blue</item>
    </style>

</resources>